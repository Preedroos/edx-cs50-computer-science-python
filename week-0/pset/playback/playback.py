# prompts the user for input replacing each space with three periods
phrase = input().replace(" ", "...")

# then outputs that same input with three periods between parameters
print(phrase, sep="...")
